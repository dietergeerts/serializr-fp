describe('SKIP', () => {

    it(`must be defined as a symbol, for usage of skipping properties.`);
});

describe('serialize', () => {

    it(`must serialize`);
});

describe('deserialize', () => {

    it(`must deserialize`);
});
